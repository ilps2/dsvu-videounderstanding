# dsvu 基线文档

> 日期：2026-08-20
> 版本：0.5.0
> Git commit: 74dfa92

## 项目概述

dsvu 是一个低成本视频理解插件，为 dsh agent 注册 `video_understand` 工具。

**核心功能**：
- B站链接 / BV 号 / 本地视频 → AVIS 信息层 → 摘要+问答
- Token 压缩 99.95%+，成本约 0.006 元/视频
- 支持 L0（本地）、L1（VLM 视觉摘要）、L2（时间窗证据）三级理解

## 入口和参数

### Node 入口（dsh/index.js）

```javascript
video_understand(target, questions?, noDownload?, level?, window?)
```

| 参数 | 类型 | 说明 |
|------|------|------|
| target | string | B站 URL / BV 号 / 本地视频绝对路径 |
| questions | string[] | 可选，自定义问题（默认 3 问） |
| noDownload | boolean | 本地文件置 true |
| level | string | `l0`(默认) / `l1` / `l2` |
| window | string | L2 时间窗，如 `10-30` |

### Python 引擎（engine/understand_video.py）

```bash
python3 understand_video.py <target> [--ask "问题"] [--no-download] [--level l0|l1|l2] [--window 10-30]
```

## Node → Python 调用

1. Node 通过 `spawn` 调用 Python 脚本
2. 传递参数：target、questions、noDownload、level、window
3. Python 输出 JSON 到 stdout
4. Node 解析 JSON 并返回给 dsh agent

## L0/L1/L2 实际路径

### L0（默认，完全本地）
1. 下载视频（360p）
2. 提取音频 → ASR 转写
3. 场景分类
4. 运动对象检测（MOG2 + YOLO）
5. 融合 prompt → LLM 摘要+问答

### L1（VLM 视觉摘要）
1. L0 基础上
2. 抽取 3-5 帧关键帧
3. 发送到 MiMo API 进行 VLM 分析
4. 补充视觉信息

### L2（时间窗证据）
1. L0 基础上
2. 用户指定时间窗
3. 密集抽帧
4. 发送到 MiMo API 进行时间线分析

## 输出 JSON 结构

```json
{
  "video": "视频标题或路径",
  "duration_s": 120.5,
  "elapsed_s": 25.3,
  "info_tokens": 1200,
  "orig_frame_tokens": 5400000,
  "token_compression_pct": 99.98,
  "cost_cny": 0.006,
  "prompt_cache_hit_tokens": 0,
  "layer_cached": false,
  "suggest_layer": false,
  "answers": [
    {
      "question": "核心内容是什么？",
      "answer": "..."
    }
  ]
}
```

## 缓存位置

- AVIS 缓存：`~/.dsh/cache/video-understand/`
- 语义层：`avis_cache/<hash>/<tag>/`

## 依赖

### 核心依赖（engine/requirements.txt）
- requests
- faster-whisper
- opencv-python
- numpy
- torch
- transformers

### 语义层依赖（engine/requirements-layer.txt）
- torch
- transformers
- sentence-transformers
- ultralytics (YOLO)

## 测试结果

### 现有测试
- `test/tools.test.mjs`: Node 工具测试
- `.github/workflows/test.yml`: CI 测试

### 测试命令
```bash
npm test           # Node 测试
pytest -q          # Python 测试（未配置）
```

## 已知问题

1. Python 测试框架未配置
2. 无离线 E2E 测试
3. 无基准测试
4. 无安全测试
5. 无隐私模式测试

## 环境要求

- Node.js >= 18
- Python 3.12+
- ffmpeg
- LLM API Key（MiMo 或 DeepSeek）

## 入口和输出样例

### 入口
```bash
python3 engine/understand_video.py "BV1GJ411x7h7" --no-download --level l0
```

### 输出
```json
{
  "video": "示例视频",
  "duration_s": 120.5,
  "elapsed_s": 25.3,
  "info_tokens": 1200,
  "orig_frame_tokens": 5400000,
  "token_compression_pct": 99.98,
  "cost_cny": 0.006,
  "prompt_cache_hit_tokens": 0,
  "layer_cached": false,
  "suggest_layer": false,
  "answers": [
    {
      "question": "这段视频的核心内容是什么？",
      "answer": "这是一个示例视频，展示了..."
    }
  ]
}
```